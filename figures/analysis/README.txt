This folder contains intermediate figures made by scripts
that are in the `src/analysis/` folder but which are not
run during `make`. I initialized this folder with a README
just in case you decide to run some of those analysis scripts.
They take a while and it would be very sad to get an error
because the directory doesn't exist. :)
